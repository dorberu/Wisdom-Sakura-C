#include <stdio.h>

int main()
{
        char str[4];
       
        str[0] = 'R';
        str[1] = 'e';
        str[2] = 'n';
        str[3] = 'a';

        printf(str);
		// 配列の最後に0が無くても動いてる

        return 0;
}
